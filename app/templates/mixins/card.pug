mixin card(data)
	a.card(href= data.link)
		.card__img-wrap
			.card__img(
				style=`background-image: url('${data.img}')`
				role='img'
				aria-label= data.title
				title= data.title
			)
		.title.title_weight_light.title_upper_none.card__title= data.title